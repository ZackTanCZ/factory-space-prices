# @package settings

# API and model artifact settings
model_path: models/xgboost_reduced.pkl
target_encoder_path: models/target_encoder.pkl
onehot_encoder_path: models/onehot_encoder.pkl

cors_origins:
  - "*"

# Model test RMSE from notebook 09a — used for prediction interval
model_rmse: 44.70